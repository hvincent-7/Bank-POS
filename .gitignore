# Proprietary / licensed content — do not commit.
# ICRTouch installer binaries (MSI, CAB, DLLs, BMPs) are owned by ICRTouch Ltd.
# Distributing them via git is a EULA/copyright breach. Keep any local copies
# outside the repo.
ICRTouch2024.zip
Binary/
reference/
*.msi
*.cab

# Node / PWA
node_modules
dist
build
.turbo
.next
.vite
coverage
*.log
.DS_Store
.env
.env.*
!.env.example
apps/api/prisma/dev.db
apps/api/prisma/dev.db-journal
apps/api/prisma/migrations/.DS_Store
